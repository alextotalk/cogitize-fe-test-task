// Proxied via Next.js rewrites: api.miex.one does not allow cross-origin
// requests from the browser (no Access-Control-Allow-Origin header).
export const ASSETS_API_URL = "/api/miex";
export const SWAP_API_URL = "https://devgateway.miex.one/api/swap";

export const PREVIEW_THROTTLE_MS = 600;
export const SEARCH_DEBOUNCE_MS = 300;
